# ============================================================
# Image Colorization (Classical + Regression) — minimal deps
#
# GPU note:
#   Default wheels target CUDA 12.8 (cu128), required for
#   NVIDIA Blackwell GPUs (RTX 50-series, sm_120).
#   On older GPUs / other CUDA versions, switch the index URL
#   and the torch/torchvision local-version suffix, e.g.:
#     CUDA 11.8 : --extra-index-url https://download.pytorch.org/whl/cu118
#                 torch==2.7.1+cu118  torchvision==0.22.1+cu118
#     CUDA 12.1 : --extra-index-url https://download.pytorch.org/whl/cu121
#                 torch==2.7.1+cu121  torchvision==0.22.1+cu121
#     CPU only  : --extra-index-url https://download.pytorch.org/whl/cpu
#                 torch==2.7.1        torchvision==0.22.1
#
# Install:
#   pip install -r requirements.txt
# ============================================================

--extra-index-url https://download.pytorch.org/whl/cu128

# --- PyTorch (CUDA 12.8 / Blackwell) ---
torch==2.7.1+cu128
torchvision==0.22.1+cu128

# --- Core scientific stack ---
numpy==2.2.6
matplotlib==3.10.8
scikit-image==0.25.2
pillow==12.0.0
tqdm==4.67.3

# --- Backbone UNet (fastai DynamicUnet + ResNet18) ---
# ipython is an implicit runtime dep of fastprogress/fastai,
# must be listed explicitly to avoid silent fallback to plain U-Net.
fastai==2.8.7
ipython==8.38.0
